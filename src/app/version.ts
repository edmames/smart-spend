/**
 * SmartSpend — single source of truth for the application version label shown
 * in Settings. Pulled from package.json at build time so it stays true to the
 * actual published version.
 */
export const appVersion = "0.1.0";
